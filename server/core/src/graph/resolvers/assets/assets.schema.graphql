type Query {
  asset(id: ID!): Asset
  assets: [Asset!]!
}

type Asset {
  createdAt: DateTime!
  id: ID!
  mimetype: String
  originalFilename: String!
  public: Boolean!
  publicUrl: String
  status: String!
  title: String!
  updatedAt: DateTime!
  url: String!
}
